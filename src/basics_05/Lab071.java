package src.basics_05;

public class Lab071 {
    public static void main(String[] args) {
        //Condition
        //-> If, If else, If Else-If, Else, switch(will use break in switch)
        //-> It checks for some condition, true or false
        // No break or continue

        //Loops
        //-> Do perform a multiple tasks
        //-> for, while, do while
        //break and continue can be used.

        //for loop
        //Use to perform a task multiple times
        //Based on condition - repeat the task multiple times
        //for(A, B, C)
        //A -> Initialization > JVM asks what variable, where is you are starting point
        //B -> Condition > when you want this to stop
        //C -> Increment/Decrement

        //A -> int i=1
        //B -> i <=10
        //C -> i++ post increment
        //D -> which code -> sout statement

        //for(A,B,C)
        //    {
        //         D
        //    }

        for(int i=1; i<=10; i++)
        {
            System.out.println("Usha");
        }
    }
}
